apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airpulse-chart.fullname" . }}-test-connection"
  labels:
    {{- include "airpulse-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airpulse-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
